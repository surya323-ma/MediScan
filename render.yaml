services:
  - type: web
    name: mediscan
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn dp_project.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
